<!DOCTYPE html>
<html lang="ar" dir="rtl"> <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عبد الرؤوف أسطورة الشطرنج — Chess Engine</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400&family=IBM+Plex+Mono:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        /* سأبقي على الستايل الرائع الذي اخترته مع تعديلات طفيفة */
        :root {
            --ink: #0a0a0a;
            --cream: #f0ebe0;
            --gold: #c9a84c;
            --dim: #2a2520;
            --mid: #5a5248;
        }

        body {
            background: var(--ink);
            color: var(--cream);
            font-family: 'IBM Plex Mono', 'Courier New', monospace;
            margin: 0;
            cursor: crosshair;
        }

        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
        }

        .eyebrow {
            font-size: 0.8rem;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 15px;
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 6vw, 5rem);
            font-weight: 900;
            margin: 0;
            line-height: 1.2;
        }

        h1 em {
            font-style: italic;
            color: var(--gold);
            display: block; /* لجعل الاسم في سطر منفصل وبحجم كبير */
        }

        .subtitle {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--mid);
            margin-top: 20px;
        }

        .board-static {
            font-size: 4rem;
            margin: 30px 0;
            filter: drop-shadow(0 0 10px var(--gold));
        }

        .btn-primary {
            background: var(--gold);
            color: var(--ink);
            padding: 15px 40px;
            text-decoration: none;
            font-weight: bold;
            margin-top: 40px;
            border-radius: 2px;
            transition: 0.3s;
        }

        .btn-primary:hover {
            transform: scale(1.05);
            background: #e0bb60;
        }
    </style>
</head>
<body>

<section class="hero">
    <p class="eyebrow">The Mastermind Behind the Code</p>
    
    <h1>
        المطور <em>عبد الرؤوف أسطورة الشطرنج</em>
    </h1>

    <div class="board-static">♔</div>

    <p class="subtitle">محرك شطرنج بني من الصفر بأيدي عربية</p>
    
    <p style="max-width: 600px; color: var(--mid); line-height: 1.6;">
        هذا الموقع يمثل القوة الحسابية والذكاء الاصطناعي في عالم الشطرنج، 
        تم تطويره برؤية تقنية تهدف للوصول إلى مستوى الأساتذة الكبار (Grandmasters).
    </p>

    <a href="#github" class="btn-primary">استعرض الكود البرمجي</a>
</section>

</body>
</html>
